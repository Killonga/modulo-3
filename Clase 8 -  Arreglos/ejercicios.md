## 📌 Ejercicio 1: Gestión de Productos en una Tienda Online

Una tienda llamada **Quinta Store** necesita manejar un listado de productos disponibles para la venta.

### **Instrucciones**
1. Crea un arreglo llamado `productos` con al menos **5 nombres de productos**.
2. Muestra en consola:
   - La **cantidad total** de productos.
   - El **primer** y el **último** producto.
3. Agrega un nuevo producto al final del arreglo.
4. Elimina el producto ubicado en la posición 2.
5. Recorre el arreglo usando un **for** e imprime:  
   `Producto disponible: <nombre>`.

---

## 📌 Ejercicio 2: Sistema de Citas Médicas

Una consulta médica necesita gestionar las horas reservadas de pacientes.

### **Instrucciones**
1. Crea un arreglo llamado `citas` que contenga al menos **5 horas reservadas** (por ejemplo `"09:00"`, `"10:30"`).
2. El médico libera una hora, por lo que debes **eliminar** la hora `"10:30"` del arreglo (si existe).
3. Agrega una nueva hora `"15:00"` al arreglo.
4. Recorre el arreglo con un **for** para mostrar todas las horas disponibles.
5. Declara una variable con una hora a buscar (por ejemplo `"09:00"`) e imprime si:
   - **Está reservada**, o  
   - **No se encuentra en el sistema**.

---

## 📌 Ejercicio 3: Inventario de Automotora

Una automotora administra distintos tipos de autos.

### **Instrucciones**
1. Crea un arreglo `autos` con **5 marcas de autos**.
2. Crea un arreglo `autosVendidos` con **2 marcas** que ya fueron vendidas.
3. A partir de ambos arreglos:
   - Genera un nuevo arreglo `autosDisponibles` que excluya los autos vendidos.
   - Recorre `autosDisponibles` con un **for** y muestra:  
     `Auto disponible para venta: <marca>`.
4. Agrega un nuevo auto a `autosDisponibles`.
5. Muestra cuántos autos están disponibles actualmente.

---